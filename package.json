{
  "name": "desec-ddns",
  "version": "1.0.0",
  "description": "Update desec.io A record to current public IP (Dockerized)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.12",
    "typescript": "^5.5.3"
  },
  "private": true
}
